
const DATA_INITIAL_APP = {
    dark: false
};

const App = (state = DATA_INITIAL_APP, action) => {
    switch (action.type) {

        default: return state;
        break;
        
    }
}

export default App; 